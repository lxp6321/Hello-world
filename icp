icp算法：（iterator closest point迭代最近点）
1.三维点云配准算法
2.PR（点云）与PB（点云）之间的旋转平移矩阵，即，求将点云PR旋转平移到与PB重叠的R和T。 
算法步骤：
1.根据待匹配点云PR={PR1,PR2,....PRn}的点坐标，在模板点云PB={PB1,PB2,...PBn}中搜索相应最近点的点集；Q={q1,q2,....qn}.

    两点间的距离平方和为dis.
    dis=(Xrj-Xbi)2+(Yrj-Ybi)2
    其中，j遍历整个点集PB,通过不断比较，找到距离最近的点，根据该点序列号保存该点集Q中，记为qi
 2.算两个点集PR、Q的重心坐标d(x_,y_)、m(x_,y_)
    x_=(Xk!)/n
    y_=(Yk!)/n
    其中，k为数据点集中的当前点序号，n为为数据点集总数。并将点集中的所有数据点扣除重心点坐标生产新的点集D、M。
 3.由新的点集计算协方差矩阵，并求解由它组成的一个四元数矩阵的最大特征值及其最大特征向量。
    新点集的协方差矩阵为：
        Cov=D M={Sxx Sxy Sxz,Syx Syy Syz,Szx Szy Szz}
 4.由于最大特征向量等价于残差平方和最小时的旋转四元数，将四元数转换为旋转矩阵R.
 5.在旋转矩阵R被确定后，由平移向量T仅仅是两个点集的重心差异，可以通过两个坐标系中的重心点和旋转矩阵确定
      我们将两个重心坐标写出向量形式，分别为：
        d_=(xd,yd,0),m_=(xm,ym,0)
      则平移向量T的计算：
        T=m_-(Rd_)
 6.将待匹配数据点集PR按照计算的旋转矩阵R和平移矩阵T变换后形成新的点集PR，通过新的点集PR与模板的邻近点集Q计算所有对应数据点距离平方和值除以数据点总数
    的值I作为迭代判断数值。
 7.迭代判断数值I小于阀值时，ICP配准算法就停止迭代，否则重复1-6步
